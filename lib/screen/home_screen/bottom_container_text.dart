import 'package:flutter/material.dart';

class BottomContainerText extends StatelessWidget {
  const BottomContainerText({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
